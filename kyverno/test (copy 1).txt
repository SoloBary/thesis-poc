================================================================
KYVERNO POLICY ENFORCEMENT TESTS — kube-goat
================================================================

# Validate enforcement against the actual kube-goat workload (hunger-check)
kubectl get deployment hunger-check-deployment -n big-monolith -o yaml > /tmp/hunger-check.yaml
kubectl delete deployment hunger-check-deployment -n big-monolith
kubectl apply -f /tmp/hunger-check.yaml -n big-monolith

# Expected output:
# Error from server: admission webhook "validate.kyverno.svc-fail" denied the request:
# require-non-root: autogen-require-non-root failed at path
#   /spec/template/spec/containers/0/securityContext/
# require-resource-limits: autogen-require-limits failed at path
#   /spec/template/spec/containers/0/resources/limits/

# Evidence: kyverno/kyverno-hunger-check.png